# Python packages
exa-py>=2.0
sentence-transformers>=3.0
requests>=2.20
tomli>=2.0; python_version < "3.11"
textual>=1.0
websockets>=12.0
pyperclip
flake8>=7.0
pytest>=9.0

# System dependencies (brew install / apt-get):
#   ripgrep (rg) — fast search, used by search_files
