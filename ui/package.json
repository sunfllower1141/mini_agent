{
  "name": "mini-agent-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Ink (React-for-CLI) frontend for mini_agent",
  "type": "module",
  "bin": {
    "mini-agent-ui": "dist/cli.js"
  },
  "scripts": {
    "build": "esbuild src/cli.tsx --bundle --platform=node --target=node20 --format=esm --outfile=dist/cli.js --packages=external --jsx=automatic --banner:js=\"#!/usr/bin/env node\\nimport { createRequire } from 'module'; const require = createRequire(import.meta.url);\" && chmod +x dist/cli.js",
    "dev": "node --loader tsx src/cli.tsx",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ink": "^5.0.1",
    "ink-spinner": "^5.0.0",
    "ink-syntax-highlight": "^2.0.2",
    "ink-text-input": "^6.0.0",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.3",
    "esbuild": "^0.23.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
