# mini_agent — Architecture & State

A coding agent powered by DeepSeek V4 Pro. Terminal REPL + Textual TUI + Electron app.
717 tests, 31 tools, SQLite memory.

## Current Architecture

```
mini_agent.py        Terminal REPL entry point
tui.py               Textual TUI (AgentWorker, themes, tree, diff preview)
electron_app/        Electron desktop app (main.js, preload.cjs, index.html)
electron_bridge.py   JSON-RPC bridge for Electron (stdin/stdout)
llm.py               LLM turn orchestration (_execute_tools, _inject_context, run_agent_turn)
api.py               API calls (call_deepseek, truncate_content, format_tool_detail)
prompt.py            System prompt + .mini_agent.rules injection
config.py            AgentConfig (.env + TOML + env + CLI priority)
memory.py            SQLite conversation store + pruning
safety.py            Read/Write safety gates + DiffPreview
terminal.py          ANSI colour helpers (DIM, c, format_table)
retry.py             HTTP retry with jitter
stream.py            SSE stream parser
agent_runtime.py     SubAgentResult + AgentRuntime (task registry, file reservations, inboxes, subscriptions, snapshots)
sub_agent.py         run_sub_agent() — sub-agent loop with turn budget, file coordination, heartbeats
interject.py         Thread-safe user interjection queue
tools/
  __init__.py        ToolResult, dispatch, cache, JSON repair, FILE_RESERVATIONS with lock
  schema.py          TOOLS list (31 tools)
  file_ops.py        read/write/edit/list/info — ALL writes check _FILE_RESERVATIONS for agent collisions
  shell_ops.py       run_shell, search_files, run_tests, git, task_status, verify
  search_ops.py      find_symbol, find_usages, semantic_search, web_search
  agent_ops.py       spawn/status/collect/collect_any/message/read/extend/handoff/inbox/subscribe/cancel + recall_turn/plan/plan_status/write_scratchpad/diff/restore_file
  agent_messages.py  AgentMessage, 9 message types, validation, routing
  agent_patterns.py  fan_out, fan_in, pipeline, barrier, scatter_gather
  lsp.py             LSP client (pylsp integration, 4 tools)
  mcp_client.py      MCP client (stdio JSON-RPC, tool discovery)

## Key State (2025-07-17)

### API Keys & Security
- NO hardcoded keys in source. All keys via .env file (DEEPSEEK_API_KEY, EXA_API_KEY, OPENAI_API_KEY).
- .env is .gitignore'd. Priority: CLI > env var > .env > TOML > default.
- max_tokens lowered 800k → 200k for cost savings.

### Inter-Agent Communication (FULLY IMPLEMENTED)
- 9 message types: text, handoff.result, handoff.request, handoff.ack, status.heartbeat, status.error, coord.fan_out, coord.fan_in, coord.sync
- Tools: agent_message, agent_read, agent_handoff, agent_inbox, agent_subscribe
- Routing: direct target delivery, subscription-based, broadcast
- Sub-agents instructed to send heartbeats every ~3 turns + check inbox each turn
- Shared file coordination rules in sub-agent system prompt

### File Reservation System (FULLY IMPLEMENTED)
- _FILE_RESERVATIONS dict (path → task_id) with threading.Lock
- _current_agent_id (threading.local) set per sub-agent
- write_file and edit_file BOTH check reservations and block cross-agent collisions
- reserve_file(), release_file(), release_all_files() in tools/__init__.py
- AgentRuntime releases all files on task completion

### Electron App
- Working JSON-RPC bridge (init, chat, cancel methods)
- Token streaming from backend to frontend
- CommonJS preload.cjs (ESM preload.js did not load)
- Uses venv/bin/python3 (system python3 lacked deps)
- CSP allows unsafe-inline scripts

### Configuration
- .env file for API keys (recommended, gitignored)
- .mini_agent.toml for advanced settings (MCP servers, overwrite flags)
- Priority: CLI flags > env vars > .env file > .mini_agent.toml > defaults

## Recent Sessions

### 2025-07-17
- Electron app fully wired (JSON-RPC bridge, init/chat/token streaming)
- API key security: removed hardcoded key, added .env loading, lowered max_tokens
- Sub-agent system prompt enhanced with shared file coordination rules
- FILE_RESERVATIONS system confirmed fully wired into write_file/edit_file
- Multiple Electron fixes: CSP, preload.cjs, venv python, dict unpacking, stdout buffering

### 2025-07-17 (Session 2 — Memory Compression/Pruning Review)
- Comprehensive review of memory.py compression/pruning pipeline
- Added 3 new test files: test_memory_compression.py (39 tests), test_memory_internals.py (29 tests), test_memory_summarize.py (22 tests)
- Total memory tests: 25 → 115 (25 original + 90 new)
- BUG FIX: _prune_by_tokens `range(start, len(messages))` → `range(start + 1, len(messages))` — was breaking immediately when first message was a user, preventing ALL token-budget pruning
- Updated prompt.py Orchestrator mode: added "LLM generation is SLOW — wait MINUTES" rule to prevent premature sub-agent cancellation
- Bumped prompt length test limit 17500 → 18500
- All 115 memory + 14 prompt tests pass (4 pre-existing unrelated failures in agent_loop + TUI)

### 2025-07-17 (Session 3 — Architecture Audit + Fixes)
- Full architecture audit across 4 reports (audit_core.md, audit_tools.md, audit_agent_system.md, audit_io.md)
- CRITICAL fixes: scatter_gather param name, read_image workspace bypass
- HIGH fixes: electron_bridge handler try/except, fan_in timeout starvation, subscription edge case, file reservation leak on crash, stream.py timeout doc
- MEDIUM/LOW: deleted _inject_token_budget no-op from 3 files, deleted dead imports from llm/api, used named constants in _summarize_pruned, extracted _SKIP_REF_NAMES module constant, deleted _sem_chunk_py dead code
- New test files from session 2: test_memory_compression.py (39), test_memory_internals.py (29), test_memory_summarize.py (22) — 115 total memory tests
- Memory bug fixed: _prune_by_tokens range(start) → range(start+1)
- Prompt updated: LLM generation patience rule for sub-agents

### 2025-07-17 (Session 4 — Test Fixes + Sub-agent Pruning)
- Fixed 3 pre-existing test failures: agent_loop budget injection (now no-op, removed from tests), TUI handle_clear (now 2 system messages)
- CRITICAL: Added periodic compression+pruning to sub-agent loop — sub-agents previously grew unbounded messages causing API 400 errors. Now calls _compress_tool_results + _prune_by_tokens every 5 turns when >20 messages, with summary injection.
- Full suite: 817 passed, 0 failed
