# mini_agent Architecture State
# Last updated: 2026-06-06 (context engineering: codebase_map + compaction)
# Last updated: 2026-06-06 (electron: fix hang on exit — app.quit() on window-all-closed)
# Last updated: 2026-06-05 (tools & memory audit — save() decomposed, AgentContext extracted,
#   orphan stripping unified in memory_prune, dead code removed)
#
# This file is the agent's map of the codebase. Update it after every
# completed change. The agent reads this at session start for context.

## Module Map

### Core Orchestration
- `core/bootstrap.py` — entry point, session init, workspace setup
- `core/llm.py` — main agent loop: turn management, tool dispatch, API calls (825 lines)
- `core/codebase_map.py` — structural codebase map: AST-based symbol extraction,
  module grouping, import graph. Injected into startup context for zero-cost
  codebase orientation. (new 2026-06-06)
- `core/context_inject.py` — per-turn context injection (handoff, state, tasks, scratchpad, git diff,
  orchestration, progress checks, circuit breaker, conversation compaction, edit risk briefing, pattern rules) (1250 lines)
- `core/prompt.py` — system prompt assembly, startup context (file tree, codebase map, git log)
- `core/config.py` — TOML config loading, provider settings (718 lines)

### Sub-Agent System
- `agents/agent_runtime.py` — sub-agent runtime, broadcast inbox, turn budgets
- `agents/sub_agent.py` — sub-agent task delegation and result collection (806 lines)

### API & Communication
- `api.py` — OpenAI/Anthropic API client abstraction; imports orphan stripping from memory_prune
- `stream.py` — streaming response handling
- `retry.py` — retry logic with exponential backoff

### Memory & Knowledge
- `memory/memory.py` — SQLite-backed memory store, scratchpad, learnings, handoff.
  save() decomposed into _prepare_messages(), _write_messages(), _maybe_vacuum().
- `memory/memory_prune.py` — message pruning, content-aware compression, conversation
  summarization, and canonical orphaned-tool-message stripping (moved from api.py).
  Dead _summarize_pruned_llm removed.
- `memory/session.py` — session lifecycle and persistence
- `interject.py` — user interjection polling
- `logging_setup.py` — structured logging

### Safety & Security
- `core/safety.py` — read/write safety gates, workspace boundaries

### Tools (tools/ directory)
- `tools/__init__.py` — tool dispatch, execute_tool, register/summarize decorators (~540 lines;
  AgentContext and file reservations extracted to dedicated modules)
- `tools/context.py` — AgentContext, _ContextProxy, _TOOL_CONTEXT, set_context (extracted from __init__)
- `tools/reservations.py` — file reservation system for sub-agent write collision prevention (extracted from __init__)
- `tools/result.py` — ToolResult dataclass
- `tools/schema.py` — tool JSON schema definitions (1559 lines)
- `tools/skills.py` — lazy tool loading via skill gates (11 core tools, 11 skill groups)
- `tools/error_hints.py` — error hint generation and failure fingerprinting
- `tools/file_ops.py` — read_file, write_file, edit_file, list_directory, file_info (1157 lines)
- `tools/shell_ops.py` — run_shell, search_files, run_tests, verify, git (1145 lines)
- `tools/search_ops.py` — find_symbol, find_usages, semantic_search, web_search (980 lines)
- `tools/agent_ops.py` — sub-agent lifecycle tools: spawn, collect, extend, cancel (1274 lines)
- `tools/agent_messages.py` — sub-agent typed messaging inbox/read (614 lines)
- `tools/agent_patterns.py` — parallel orchestration patterns: fan_out, pipeline, barrier (932 lines)
- `tools/agent_todos.py` — per-agent todo tracking
- `tools/failure_learning.py` — failure pattern store, self-critique, project knowledge (1260 lines)
- `tools/tool_graph.py` — tool transition graph for sequencing hints and anti-pattern detection
- `tools/lsp.py` — language server protocol tools (851 lines)
- `tools/desktop_ops.py` — desktop automation tools (1064 lines)
- `tools/macos_ops.py` — macOS-specific tools (972 lines)
- `tools/browser_ops.py` — Playwright/Puppeteer browser automation
- `tools/_json_rpc_shared.py` — shared drain_stderr() utility for LSP and MCP clients
- `tools/mcp_client.py` — MCP (Model Context Protocol) client

### Electron Desktop App
- `mini_agent_electron/` — Electron-based desktop wrapper

### Tests
- `tests/test_memory.py` — memory store tests
- `tests/test_tools.py` — tool execution tests
- `tests/test_resilience.py` — orphan stripping and message cleaning tests
- `tests/test_memory_internals.py` — _clean_messages, migration, export tests
- `tests/test_api.py` — API client and message cleaning tests
- `conftest.py` — shared test fixtures
- `eval/` — evaluation harness

### Config Files
- `.mini_agent.toml` — runtime config (API keys, model, provider)
- `.mini_agent.rules` — agent behavioral rules
- `.mini_agent_index.json` — workspace file index

### Tracking Files (self-modification audit)
- `STATE.txt` — this file, architecture map
- `TASKS.md` — task-to-file mapping index (orient yourself when starting new work)
- `HANDOFF.md` — session-to-session continuity
- `CHANGELOG.md` — structured change audit trail

## Active Decisions

1. **Context injection is one-time + per-turn**: HANDOFF.md, STATE.txt, scratchpad, and git diff
   are injected once per session. Orchestration status, interjections, and progress checks run
   every turn.
2. **Safety gate pattern**: All file and shell operations go through ReadSafetyGate and
   WriteSafetyGate in core/safety.py, enforcing workspace boundaries.
3. **Sub-agents are isolated**: Each sub-agent gets its own context, turn budget, and
   broadcast inbox. Parallel execution via agents/agent_runtime.py with auto-extension for
   productive agents.
4. **Circuit breaker**: Detects repeated identical tool calls and warns before API cost
   spirals (core/context_inject.py, 6-call window, 3-repeat threshold).
5. **Memory is SQLite**: memory/memory.py uses SQLite for persistence with separate session DBs.
   Learnings are tagged and searchable.
6. **Injection flags reset in bootstrap.init_session()**: Context injection flags
   (_scratchpad_injected, _git_diff_injected, _handoff_injected, _state_txt_injected)
   are reset once per session in core/bootstrap.init_session(), NOT per turn in
   run_agent_turn(). This gives injectors one chance per session, by design.
7. **build_startup_context lives in prompt.py**: The startup context builder is defined
   in core/prompt.py and imported directly, not re-exported through config.py.
8. **Unified orphan stripping in memory_prune.py**: ``_strip_orphaned_tool_messages``
   is the single source of truth, living in ``memory/memory_prune.py``. Both ``api.py``
   and ``memory/memory.py`` import from there. ``truncate=False`` (default) for API calls
   removes individual orphans; ``truncate=True`` for persistence truncates at the first
   incomplete sequence. Backward-compat aliases kept in api.py.
9. **Content-aware compression everywhere**: ``context_inject._compress_stale_tool_results``
   delegates to ``memory_prune._compress_tool_results`` — per-tool-type compression with
   knowledge of request parameters (offset/limit for read_file, match patterns for search_files).
10. **Cache caps**: ``_clean_messages_cache`` capped at 16 entries, ``_complexity_cache`` at 32.
    Prevents unbounded growth from stale message-list entries.
11. **MemoryStore.save() decomposed**: _prepare_messages() handles cleaning, compression,
    pruning, and summarisation. _write_messages() handles SQL writes with retry logic.
    _maybe_vacuum() handles opportunistic DB compaction. save() is a two-line dispatch.
12. **AgentContext and reservations extracted**: ``tools/context.py`` holds AgentContext,
    _ContextProxy, _TOOL_CONTEXT, and set_context. ``tools/reservations.py`` holds the
    file reservation system. Both re-exported through ``tools/__init__.py`` for
    backward compatibility.
13. **Codebase map generation**: ``core/codebase_map.py`` uses AST (Python) and regex
    (JS/TS) to extract symbols, module groupings, and import dependencies. Injected
    into startup context as a ~2K token structural map, saving the agent 3-5
    exploratory tool calls at session start.
14. **Mid-session conversation compaction**: ``_compact_if_needed()`` in
    ``context_inject.py`` runs at the start of every turn. When the conversation
    exceeds 80% of the context window, oldest messages are pruned and summarized
    using the existing ``_prune_by_tokens`` / ``_summarize_pruned`` pipeline,
    preserving system prompt + recent context. Compaction targets 70% fill ratio
    to leave room for turn growth. (new 2026-06-06)
15. **Pre-edit risk briefing**: ``_inject_edit_risk_context()`` scans the most
    recent assistant message for ``edit_file`` / ``write_file`` tool calls and
    injects a risk briefing with: git modification frequency (hotspot detection),
    co-change analysis (files modified in same commits), and missing test
    coverage. Zero-token cost when no edits are pending. (new 2026-06-06)
16. **File-pattern conditional rules**: ``.mini_agent/rules.toml`` defines
    glob-pattern rules (e.g. ``core/*.py``, ``tests/*.py``). ``_inject_pattern_rules()``
    checks recent file operations and injects only the rules matching touched
    files. Each rule injected at most once per session. Zero-token when no
    matching files are being worked on. (new 2026-06-06)
17. **Task-to-file index**: ``TASKS.md`` provides a curated mapping from common
    development tasks (\"Add a new tool\", \"Modify system prompt\", \"Change
    memory storage\") to the relevant source files. Injected once at session
    startup alongside HANDOFF.md and STATE.txt. (new 2026-06-06)

## Known Issues
- `_json_rpc_shared.py` — shared drain_stderr() utility used by lsp.py and mcp_client.py
- No semantic search across past sessions (current recall is session-only)
- Core packages (`core/`, `agents/`, `memory/`) use lazy `__init__.py` with `__getattr__` to
  break circular imports. Adding new public symbols requires updating the `__getattr__` chain.
